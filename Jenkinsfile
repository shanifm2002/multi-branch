pipeline {
    agent any

    stages {
        stage('Main Branch Stage') {
            steps {
                echo "This is the MAIN branch build."
                echo "Executing main branch pipeline..."
            }
        }
    }
}
